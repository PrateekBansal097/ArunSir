--<ScriptOptions statementTerminator=";"/>

CREATE TABLE country (
	COUNTRY_ID INT NOT NULL,
	COUNTRY_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	PRIMARY KEY (COUNTRY_ID)
) ENGINE=InnoDB;

CREATE TABLE party (
	PARTY_ID INT NOT NULL,
	PARTY_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	PRIMARY KEY (PARTY_ID)
) ENGINE=InnoDB;

CREATE TABLE sector (
	SECTOR_ID INT NOT NULL,
	SECTOR_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	PRIMARY KEY (SECTOR_ID)
) ENGINE=InnoDB;

CREATE TABLE publication (
	PUBLICATION_ID INT NOT NULL,
	PUBLICATION_NAME VARCHAR(50),
	PUBLICATION_SHORT_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	STATE_ID INT,
	PRIMARY KEY (PUBLICATION_ID)
) ENGINE=InnoDB;

CREATE TABLE city (
	CITY_ID INT NOT NULL,
	CITY_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	STATE_ID INT,
	IS_DELETED INT,
	PRIMARY KEY (CITY_ID)
) ENGINE=InnoDB;

CREATE TABLE state (
	STATE_ID INT NOT NULL,
	STATE_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	COUNTRY_ID INT,
	IS_DELETED INT,
	PRIMARY KEY (STATE_ID)
) ENGINE=InnoDB;

CREATE TABLE client (
	CLIENT_ID INT NOT NULL,
	CLIENT_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	PRIMARY KEY (CLIENT_ID)
) ENGINE=InnoDB;

CREATE TABLE registration (
	ID INT NOT NULL,
	ADDRESS VARCHAR(255),
	EMAIL VARCHAR(255),
	FIRST_NAME VARCHAR(255),
	LAST_NAME VARCHAR(255),
	CONTACT DOUBLE,
	PASSWORD VARCHAR(255),
	SEX VARCHAR(255),
	IS_DELETED INT,
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	USER_TYPE_ID INT,
	PRIMARY KEY (ID)
) ENGINE=InnoDB;

CREATE TABLE tracking (
	TRACKING_ID INT NOT NULL,
	SLUG VARCHAR(1000),
	TIME VARCHAR(30),
	NEWS_TYPE_ID INT,
	CLIENT_ID INT,
	SECTOR_ID INT,
	CITY_ID INT,
	SUB_SECTOR_ID INT,
	NEWS_TREND VARCHAR(50),
	STORY_CODE VARCHAR(50),
	MARKING INT,
	CHANNEL_ID INT,
	STATE_ID INT,
	DATE DATE,
	IS_DELETED INT,
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	RADIO VARCHAR(50),
	PRIMARY KEY (TRACKING_ID)
) ENGINE=InnoDB;

CREATE TABLE advertisement (
	ADVERTISEMENT_ID INT NOT NULL,
	SLUG VARCHAR(1000),
	ADV_TYPE_ID INT,
	PARTY_ID INT,
	DURATION VARCHAR(50),
	CLIENT_ID INT,
	STATE_ID INT,
	DATE DATE,
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	CITY_ID INT,
	END_TIME VARCHAR(255),
	START_TIME VARCHAR(255),
	CHANNEL_ID INT,
	RADIO VARCHAR(50),
	PRIMARY KEY (ADVERTISEMENT_ID)
) ENGINE=InnoDB;

CREATE TABLE newstype (
	NEWS_TYPE_ID INT NOT NULL,
	NEWS_TYPE_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	PRIMARY KEY (NEWS_TYPE_ID)
) ENGINE=InnoDB;

CREATE TABLE channel (
	CHANNEL_ID INT NOT NULL,
	CHANNEL_NAME VARCHAR(50),
	CHANNEL_SHORT_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	STATE_ID INT,
	IS_DELETED INT,
	PRIMARY KEY (CHANNEL_ID)
) ENGINE=InnoDB;

CREATE TABLE printtracking (
	PRINT_TRACKING_ID INT NOT NULL,
	SLUG VARCHAR(1000),
	PAGE INT,
	NEWS_COLUMN INT,
	CITY_ID INT,
	STATE_ID INT,
	SUB_SECTOR_ID INT,
	NEWS_TREND VARCHAR(50),
	STORY_CODE VARCHAR(50),
	MARKING INT,
	PUBLICATION_ID INT,
	DATE DATE,
	CLIENT_ID INT,
	SECTOR_ID INT,
	PHOTO_COLUMN INT,
	IS_DELETED INT,
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	PHOTO VARCHAR(10),
	RADIO VARCHAR(50),
	PRIMARY KEY (PRINT_TRACKING_ID)
) ENGINE=InnoDB;

CREATE TABLE subsector (
	SUB_SECTOR_ID INT NOT NULL,
	SUB_SECTOR_NAME VARCHAR(50),
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	IS_DELETED INT,
	PRIMARY KEY (SUB_SECTOR_ID)
) ENGINE=InnoDB;

CREATE TABLE advtype (
	ADV_TYPE_ID INT NOT NULL,
	ADV_TYPE_NAME VARCHAR(50),
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	IS_DELETED INT,
	PRIMARY KEY (ADV_TYPE_ID)
) ENGINE=InnoDB;

CREATE TABLE popup (
	POP_ID INT NOT NULL,
	MESSAGE VARCHAR(1000),
	DATE DATE,
	IS_DELETED INT,
	CREATED_BY VARCHAR(50),
	CREATED_DATE DATE,
	UPDATED_BY VARCHAR(50),
	UPDATED_DATE DATE,
	PRIMARY KEY (POP_ID)
) ENGINE=InnoDB;

CREATE INDEX PRINT_SUB_SECTOR_ID ON printtracking (SUB_SECTOR_ID ASC);

CREATE INDEX CLIENT_ID ON tracking (CLIENT_ID ASC);

CREATE INDEX CITY_ID ON city (STATE_ID ASC);

CREATE INDEX TRACKING_CITY_ID ON tracking (CITY_ID ASC);

CREATE INDEX SUB_SECTOR_ID ON tracking (SUB_SECTOR_ID ASC);

CREATE INDEX PARTY_ID ON advertisement (PARTY_ID ASC);

CREATE INDEX PRINT_STATE_ID ON printtracking (STATE_ID ASC);

CREATE INDEX PUBLICATION_ID ON printtracking (PUBLICATION_ID ASC);

CREATE INDEX ADV_CLIENT_ID ON advertisement (CLIENT_ID ASC);

CREATE INDEX PRINT_CITY_ID ON printtracking (CITY_ID ASC);

CREATE INDEX ADV_TYPE_ID ON advertisement (ADV_TYPE_ID ASC);

CREATE INDEX STATE_ID ON state (COUNTRY_ID ASC);

CREATE INDEX TRACKING_SECTOR_ID ON tracking (SECTOR_ID ASC);

CREATE INDEX ADV_CITY_ID ON advertisement (CITY_ID ASC);

CREATE INDEX NEWS_TYPE_ID ON tracking (NEWS_TYPE_ID ASC);

CREATE INDEX CHANNEL_ID ON tracking (CHANNEL_ID ASC);

CREATE INDEX FKF85DD20534CEC20F ON advertisement (CHANNEL_ID ASC);

CREATE INDEX CHANEEL_STATE_ID ON channel (STATE_ID ASC);

CREATE INDEX TRACKING_STATE_ID ON tracking (STATE_ID ASC);

CREATE INDEX STATED_ID ON publication (STATE_ID ASC);

CREATE INDEX ADV_STATE_ID ON advertisement (STATE_ID ASC);

CREATE INDEX PRINT_CLIENT_ID ON printtracking (CLIENT_ID ASC);

CREATE INDEX PRINT_SECTOR_ID ON printtracking (SECTOR_ID ASC);

